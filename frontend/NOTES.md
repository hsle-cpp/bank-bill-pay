## Overview

React Native Web Frontend
    - Given a task Implement as Library components
    - avoid writing into libraries that you do not own
    - Build your library component as a isolated fail safe if possible